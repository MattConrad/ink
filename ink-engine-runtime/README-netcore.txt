This is an altered version of Ink's source code with a few changes.

Target build is revised from the PCL to .NET Standard 1. 0 (which maps to .NET Core 1.0).

Every "internal" access modifier changed to "public" via find/replace.
